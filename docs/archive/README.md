# Preserved baseline

The files listed in `SHA256SUMS` come from commit
`468d66d02af567cdd7244f6f8f19c4310bcef3df`. The root README is preserved here
as `README.original.md`; all scripts, data and figures remain at their original paths.
Links inside the archived README describe the original root layout.

From the repository root, verify the complete baseline without running a simulation:

```sh
shasum -a 256 -c docs/archive/SHA256SUMS
```

These checks establish file integrity, not a rerun of the experiments.
